#include <cmath>
#include <iostream>

using namespace std;

int main() {
  /* float x, y = 10, z = 5, a = 12, b = 3;
  x = y + pow(z, 3) + (a / b);
  cout << x << endl; */

  float x;
  float y = 1 + (3 * 4) - 15;
  float z = pow(4, 2) - pow(3, 2);
  x = y / z;
  cout << x << endl;

  return 0;
}
